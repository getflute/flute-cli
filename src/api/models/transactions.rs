//! Request/response DTOs for the Transactions API group
//! (`/pay-api/v1/transactions/*`).
//!
//! Field names match the OpenAPI wire format exactly (camelCase).
//! Populated in Tasks 1.3+; the module exists now so `client/transactions.rs`
//! can import from it without a structural change.
