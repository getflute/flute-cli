//! Shared enums and sub-DTOs used across multiple API groups
//! (e.g. CardDataSource, Currency, Address, ContactInfo).
//!
//! Populated in later tasks; the module exists now so sibling modules can
//! `use super::common::*` without a structural change.
